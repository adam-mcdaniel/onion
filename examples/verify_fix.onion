(println "1 + ! 1 = " (1 + ! 1))
(println "! 1 + 1 = " (! 1 + 1))
(println "! (1 + 1) = " (! {1 + 1})) ; using block for grouping if parens make list
; actually (1 + 1) is list [3] (evaluates to list of one element).
; if ! expects number, ! [3] is error.
; if ! expects list? No, ! is negate.

; Test just the operators
(def x 1)
(println "!x = " !x)
